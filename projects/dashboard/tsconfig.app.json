{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "../../out-tsc/app",
    "types": ["chrome", "services", "serializers", "crypto-js"]
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
